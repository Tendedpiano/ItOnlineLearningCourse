function changeHeadingText() {
    // Targets the heading by its unique id
    var headingElement = document.getElementById("myheading");
    // Assign new text to the heading element
    headingElement.textContent = "New Heading Text";
}